// @ts-check
import { defineConfig } from 'astro/config';

// URL pública del sitio. Cuando tengan dominio propio, cambiar "site" (ej: https://piagallardo.cl)
export default defineConfig({
  site: 'https://pia-gallardo.workers.dev',
});
